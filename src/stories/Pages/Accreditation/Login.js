import React from 'react'

import LoginContainer from '../../../Containers/Accreditation/Login'

export default {
  title: 'Pages',
  component: LoginContainer
}

const Template = () => <LoginContainer />

export const Login = Template.bind({})
